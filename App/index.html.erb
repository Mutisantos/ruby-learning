<h1>Hola!</h1>
<%# Con %= lo procesado en ruby se muestra en el HTML %>
<%= @mensaje %>

<%# Sin esto, solo se procesa pero no se muestra en la pagina  %>
<% @posts.each do |post| %>
    <h2> <%= post['title'] %> </h2>
    <p> <%= post['body'] %> </p>
    <hr/>
<% end %>
